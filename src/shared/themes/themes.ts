import { buttonTheme } from './buttons';
import { grayTheme, neutralTheme, primaryTheme, secondaryTheme } from './colors';

export const Theme = {
  colors: {
    grayTheme,
    neutralTheme,
    primaryTheme,
    secondaryTheme,
  },
  buttons: {
    buttonTheme,
  },
};
