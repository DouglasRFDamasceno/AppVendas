import { buttonTheme } from './buttons';
import {
  grayTheme,
  greenTheme,
  neutralTheme,
  primaryTheme,
  redTheme,
  secondaryTheme,
} from './colors';

export const Theme = {
  colors: {
    grayTheme,
    neutralTheme,
    primaryTheme,
    secondaryTheme,
    greenTheme,
    redTheme,
  },
  buttons: {
    buttonTheme,
  },
};
