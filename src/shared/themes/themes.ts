import { buttonTheme } from './buttons';
import { grayTheme, greenTheme, neutralTheme, primaryTheme, secondaryTheme } from './colors';

export const Theme = {
  colors: {
    grayTheme,
    neutralTheme,
    primaryTheme,
    secondaryTheme,
    greenTheme,
  },
  buttons: {
    buttonTheme,
  },
};
